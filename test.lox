fun fib(n) {
  if (n < 2) return n;
  return fib(n - 1) + fib(n - 2); 
}

var x = 20;

while (x <= 40) {
  var before = clock();
  print x;
  print fib(x);
  x = x + 1;
  var after = clock();
  print after - before;
  print "**************";
}